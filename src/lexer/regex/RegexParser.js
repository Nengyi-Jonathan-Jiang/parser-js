import {Regex} from "./Regex";

/**
 * Parses a string into a {@link Regex}, which can be
 * compiled into an {@link NFA}
 * @param {string} src
 * @returns {Regex}
 */
export function parseRegex(src) {

}